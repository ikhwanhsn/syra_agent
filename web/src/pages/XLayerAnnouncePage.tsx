import { XLayerAnnounceDeck } from "@/components/post/photo/XLayerAnnounceDeck";

/** Square X-Layer-style announcement cards at /post/announce. */
export default function XLayerAnnouncePage() {
  return <XLayerAnnounceDeck />;
}
