import type { ReactNode } from "react";
import clsx from "clsx";
import Heading from "@theme/Heading";
import styles from "./styles.module.css";

type FeatureItem = {
  title: string;
  Svg: React.ComponentType<React.ComponentProps<"svg">>;
  description: ReactNode;
};

const FeatureList: FeatureItem[] = [
  {
    title: "Easy to Use",
    Svg: require("@site/static/img/undraw_docusaurus_mountain.svg").default,
    description: (
      <>
        $SYRA is built so anyone can deploy powerful AI agents without needing
        deep technical knowledge. Simple setup, instant execution, start
        automating your strategies in minutes.
      </>
    ),
  },
  {
    title: "Focus on What Matters",
    Svg: require("@site/static/img/undraw_docusaurus_tree.svg").default,
    description: (
      <>
        With $SYRA, you focus on your trading logic and ideas, while the system
        handles execution, monitoring, and optimization. We take care of the
        complexity so you can focus on growth.
      </>
    ),
  },
  {
    title: "Powered by Intelligence",
    Svg: require("@site/static/img/undraw_docusaurus_react.svg").default,
    description: (
      <>
        $SYRA leverages advanced AI and on-chain Solana infrastructure, allowing
        you to extend, customize, and evolve your agents effortlessly, all while
        maintaining a clean, consistent environment.
      </>
    ),
  },
];

function Feature({ title, Svg, description }: FeatureItem) {
  return (
    <div className={clsx("col col--4")}>
      <div className="text--center">
        <Svg className={styles.featureSvg} role="img" />
      </div>
      <div className="text--center padding-horiz--md">
        <Heading as="h3">{title}</Heading>
        <p>{description}</p>
      </div>
    </div>
  );
}

export default function HomepageFeatures(): ReactNode {
  return (
    <section className={styles.features}>
      <div className="container">
        <div className="row">
          {FeatureList.map((props, idx) => (
            <Feature key={idx} {...props} />
          ))}
        </div>
      </div>
    </section>
  );
}
