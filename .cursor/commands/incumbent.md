# incumbent

Create **incumbent hype** X text for Syra (no placeholders). Does **not** auto-post. Text only unless the user also asks for a still.

Disruptive four-beat spine: industry still on the old process → old cost/time vs Syra → `syra is …` → `the … are replaceable.` → real URL.

1. Follow `.cursor/agents/content-swipe/INCUMBENT_HYPE_TEXT.md` in full (Chronicle Incumbent mode).
2. Read `.cursor/agents/state/last-incumbent.json` if present. Skip `doNotRepeat` angles and hooks.
3. Ground facts from README, `api/config/x402Pricing.js`, docs, video promo copy, or `GET https://api.syraa.fun/api/metrics` when the angle is proof-shaped. Never invent.
4. Default: **1** paste-ready post + tighter + optional how-to reply. If the user asks for “more N,” deliver **N** variants on **different** shipped surfaces from the angle bank.
5. Write `.cursor/agents/state/last-incumbent.json`. Honors `TEXT_POST_QUALITY_BAR.md` named format “incumbent hype text”, legal-compliance, no invented TAM, no em dashes.

Do not run the `/ideas` board. Do not run `/hype` image pipeline. Do not force `X is live.` launch spine.
